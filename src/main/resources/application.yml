spring:
  application:
    name: api
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://db.kcigpuiqmdyszcsudump.supabase.co:5432/postgres}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:Java@137500}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

supabase:
  jwt:
    secret: ${SUPABASE_JWT_SECRET:X1+J1/1Zwm3WRg4DcBW9dIHyHayz+dkSWEFacDZ4fiesfcl/ITKvNKJ1lxYvn8NTLcFlDF+vyQk+VRSbDUpCVA==}

google:
  books:
    api-key: ${GOOGLE_BOOKS_API_KEY:AIzaSyAp6qaYN256aak7F4Jj__4mzKQ8oXJaLOM}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003}

server:
  port: ${PORT:8080}
